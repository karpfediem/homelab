cloudflared:
  credentials:
    existingSecret: cloudflared-credentials
  config:
    tunnel: homelab-udp
    ingress:
      - service: traefik.kube-system

  podMonitor:
    enabled: true
    metricsEndpoints:
      - port: http
